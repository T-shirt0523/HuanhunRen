# Screenshots

All eight frames were exported by the game itself with the headless screenshot pipeline
(`-DDEBUG_HEADLESS -DDEBUG_AUTO_SHOT`), so they are real renders at the internal
640x360 resolution — not mock-ups. Rebuild the debug target and run it to regenerate
them (see the *Development tooling* section of the README).
